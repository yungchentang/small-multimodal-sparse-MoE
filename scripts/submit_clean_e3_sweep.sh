#!/usr/bin/env bash
set -euo pipefail

# Submit one clean-data E3 candidate from the iterative sweep.
# Usage: bash scripts/submit_clean_e3_sweep.sh {base-cap6|legacy-cap4|cap8-rank9|ce-long-cap4|alignpre-direct|image-rank-cap8|contrastive-light-cap6|contrastive-imageboost-cap6|contrastive-balanced-cap8|contrastive-img15-cap6|contrastive-prefix75-cap8|contrastive-router-cap8}

CANDIDATE="${1:-}"
if [ -z "$CANDIDATE" ]; then
  echo "usage: bash scripts/submit_clean_e3_sweep.sh {base-cap6|legacy-cap4|cap8-rank9|ce-long-cap4|alignpre-direct|image-rank-cap8|contrastive-light-cap6|contrastive-imageboost-cap6|contrastive-balanced-cap8|contrastive-img15-cap6|contrastive-prefix75-cap8|contrastive-router-cap8}" >&2
  exit 2
fi

STAMP="${STAMP:-$(date +%y%m%d%H%M)}"
export DATA_DIR="${DATA_DIR:-data/real_subset_clean_260708b}"
export REAL_MAX_SOURCE_AUDIO_SECONDS="${REAL_MAX_SOURCE_AUDIO_SECONDS:-6}"
export REAL_MAX_TRANSCRIPT_WORDS="${REAL_MAX_TRANSCRIPT_WORDS:-18}"
export CAPTION_MIN_ASCII_RATIO="${CAPTION_MIN_ASCII_RATIO:-0.85}"
export CAPTION_MIN_LETTERS="${CAPTION_MIN_LETTERS:-8}"
export TEXT_EVAL_BLOCKS="${TEXT_EVAL_BLOCKS:-160}"
export RETRIEVAL_EVAL_SAMPLES="${RETRIEVAL_EVAL_SAMPLES:-250}"
export CONDITIONAL_EVAL_SAMPLES="${CONDITIONAL_EVAL_SAMPLES:-250}"
export CONDITIONAL_BATCH_SIZE="${CONDITIONAL_BATCH_SIZE:-16}"
export AUDIO_PREFIX_TOKENS="${AUDIO_PREFIX_TOKENS:-64}"
export ENCODER_FEATURE_TOKENS="${ENCODER_FEATURE_TOKENS:-100}"
export TRAIN_ROUTER_GATES="${TRAIN_ROUTER_GATES:-0}"
export EXPERT_ABLATION_STEPS=0
export ABLATION_STEPS=0
export CAPACITY_ABLATION_STEPS=0

case "$CANDIDATE" in
  base-cap6)
    export JOB_NAME="${JOB_NAME:-sparse-moe-clean-e3-base-cap6-${STAMP}}"
    export OUT="${OUT:-outputs/${JOB_NAME}}"
    export FINAL_STEPS="${FINAL_STEPS:-4000}"
    export TRAIN_BATCH_SIZE="${TRAIN_BATCH_SIZE:-4}"
    export EVAL_BATCH_SIZE="${EVAL_BATCH_SIZE:-8}"
    export MODALITY_CYCLE="${MODALITY_CYCLE:-text,image,speech,speech,speech,speech}"
    export CAPACITY_FACTOR="${CAPACITY_FACTOR:-6.0}"
    export AUX_COEF="${AUX_COEF:-0.01}"
    export TRAIN_LM_HEAD="${TRAIN_LM_HEAD:-1}"
    export LEARNING_RATE="${LEARNING_RATE:-0.0008}"
    export LM_HEAD_LEARNING_RATE="${LM_HEAD_LEARNING_RATE:-0.00001}"
    export CONDITIONAL_NEGATIVES="${CONDITIONAL_NEGATIVES:-9}"
    export CONDITIONAL_RANKING_NEGATIVES="${CONDITIONAL_RANKING_NEGATIVES:-9}"
    export CONDITIONAL_RANKING_TEMPERATURE="${CONDITIONAL_RANKING_TEMPERATURE:-0.7}"
    export IMAGE_CONDITIONAL_RANKING_COEF="${IMAGE_CONDITIONAL_RANKING_COEF:-0.5}"
    export SPEECH_CONDITIONAL_RANKING_COEF="${SPEECH_CONDITIONAL_RANKING_COEF:-3.0}"
    ;;
  legacy-cap4)
    export JOB_NAME="${JOB_NAME:-sparse-moe-clean-e3-legacy-cap4-${STAMP}}"
    export OUT="${OUT:-outputs/${JOB_NAME}}"
    export FINAL_STEPS="${FINAL_STEPS:-3000}"
    export TRAIN_BATCH_SIZE="${TRAIN_BATCH_SIZE:-8}"
    export EVAL_BATCH_SIZE="${EVAL_BATCH_SIZE:-8}"
    export MODALITY_CYCLE="${MODALITY_CYCLE:-text,image,speech,speech,speech}"
    export CAPACITY_FACTOR="${CAPACITY_FACTOR:-4.0}"
    export AUX_COEF="${AUX_COEF:-0.01}"
    export TRAIN_LM_HEAD="${TRAIN_LM_HEAD:-1}"
    export LEARNING_RATE="${LEARNING_RATE:-0.001}"
    export LM_HEAD_LEARNING_RATE="${LM_HEAD_LEARNING_RATE:-0.00001}"
    export CONDITIONAL_NEGATIVES="${CONDITIONAL_NEGATIVES:-4}"
    export CONDITIONAL_RANKING_NEGATIVES="${CONDITIONAL_RANKING_NEGATIVES:-2}"
    export CONDITIONAL_RANKING_TEMPERATURE="${CONDITIONAL_RANKING_TEMPERATURE:-1.0}"
    export IMAGE_CONDITIONAL_RANKING_COEF="${IMAGE_CONDITIONAL_RANKING_COEF:-0.5}"
    export SPEECH_CONDITIONAL_RANKING_COEF="${SPEECH_CONDITIONAL_RANKING_COEF:-2.0}"
    ;;
  cap8-rank9)
    export JOB_NAME="${JOB_NAME:-sparse-moe-clean-e3-cap8-rank9-${STAMP}}"
    export OUT="${OUT:-outputs/${JOB_NAME}}"
    export FINAL_STEPS="${FINAL_STEPS:-4000}"
    export TRAIN_BATCH_SIZE="${TRAIN_BATCH_SIZE:-4}"
    export EVAL_BATCH_SIZE="${EVAL_BATCH_SIZE:-8}"
    export MODALITY_CYCLE="${MODALITY_CYCLE:-text,image,speech,speech,speech,speech}"
    export CAPACITY_FACTOR="${CAPACITY_FACTOR:-8.0}"
    export AUX_COEF="${AUX_COEF:-0.01}"
    export TRAIN_LM_HEAD="${TRAIN_LM_HEAD:-1}"
    export LEARNING_RATE="${LEARNING_RATE:-0.0008}"
    export LM_HEAD_LEARNING_RATE="${LM_HEAD_LEARNING_RATE:-0.00001}"
    export CONDITIONAL_NEGATIVES="${CONDITIONAL_NEGATIVES:-9}"
    export CONDITIONAL_RANKING_NEGATIVES="${CONDITIONAL_RANKING_NEGATIVES:-9}"
    export CONDITIONAL_RANKING_TEMPERATURE="${CONDITIONAL_RANKING_TEMPERATURE:-0.7}"
    export IMAGE_CONDITIONAL_RANKING_COEF="${IMAGE_CONDITIONAL_RANKING_COEF:-0.5}"
    export SPEECH_CONDITIONAL_RANKING_COEF="${SPEECH_CONDITIONAL_RANKING_COEF:-3.0}"
    ;;
  ce-long-cap4)
    export JOB_NAME="${JOB_NAME:-sparse-moe-clean-e3-ce-long-cap4-${STAMP}}"
    export OUT="${OUT:-outputs/${JOB_NAME}}"
    export FINAL_STEPS="${FINAL_STEPS:-6000}"
    export TRAIN_BATCH_SIZE="${TRAIN_BATCH_SIZE:-16}"
    export EVAL_BATCH_SIZE="${EVAL_BATCH_SIZE:-8}"
    export MODALITY_CYCLE="${MODALITY_CYCLE:-text,image,speech,speech,speech}"
    export CAPACITY_FACTOR="${CAPACITY_FACTOR:-4.0}"
    export AUX_COEF="${AUX_COEF:-0.01}"
    export TRAIN_LM_HEAD="${TRAIN_LM_HEAD:-0}"
    export LEARNING_RATE="${LEARNING_RATE:-0.001}"
    export CONDITIONAL_NEGATIVES="${CONDITIONAL_NEGATIVES:-4}"
    export IMAGE_CONDITIONAL_RANKING_COEF="${IMAGE_CONDITIONAL_RANKING_COEF:-0.0}"
    export SPEECH_CONDITIONAL_RANKING_COEF="${SPEECH_CONDITIONAL_RANKING_COEF:-0.0}"
    ;;
  alignpre-direct)
    export JOB_NAME="${JOB_NAME:-sparse-moe-clean-e3-alignpre-direct-${STAMP}}"
    export OUT="${OUT:-outputs/${JOB_NAME}}"
    export FINAL_STEPS="${FINAL_STEPS:-4000}"
    export TRAIN_BATCH_SIZE="${TRAIN_BATCH_SIZE:-16}"
    export EVAL_BATCH_SIZE="${EVAL_BATCH_SIZE:-8}"
    export MODALITY_CYCLE="${MODALITY_CYCLE:-text,image,speech,speech,speech}"
    export CAPACITY_FACTOR="${CAPACITY_FACTOR:-4.0}"
    export AUX_COEF="${AUX_COEF:-0.01}"
    export TRAIN_LM_HEAD="${TRAIN_LM_HEAD:-0}"
    export LEARNING_RATE="${LEARNING_RATE:-0.00025}"
    export RETRIEVAL_HEAD_LEARNING_RATE="${RETRIEVAL_HEAD_LEARNING_RATE:-0.005}"
    export GRAD_CLIP="${GRAD_CLIP:-10.0}"
    export ALIGNMENT_PRETRAIN_STEPS="${ALIGNMENT_PRETRAIN_STEPS:-3000}"
    export CONTRASTIVE_COEF="${CONTRASTIVE_COEF:-5.0}"
    export IMAGE_CONTRASTIVE_COEF="${IMAGE_CONTRASTIVE_COEF:-5.0}"
    export SPEECH_CONTRASTIVE_COEF="${SPEECH_CONTRASTIVE_COEF:-25.0}"
    export IMAGE_CONTRASTIVE_NEGATIVES="${IMAGE_CONTRASTIVE_NEGATIVES:--1}"
    export SPEECH_CONTRASTIVE_NEGATIVES="${SPEECH_CONTRASTIVE_NEGATIVES:--1}"
    export IMAGE_CONDITIONAL_RANKING_COEF="${IMAGE_CONDITIONAL_RANKING_COEF:-0.0}"
    export SPEECH_CONDITIONAL_RANKING_COEF="${SPEECH_CONDITIONAL_RANKING_COEF:-0.0}"
    ;;

  image-rank-cap8)
    export JOB_NAME="${JOB_NAME:-sparse-moe-clean-e3-image-rank-cap8-${STAMP}}"
    export OUT="${OUT:-outputs/${JOB_NAME}}"
    export FINAL_STEPS="${FINAL_STEPS:-4500}"
    export TRAIN_BATCH_SIZE="${TRAIN_BATCH_SIZE:-4}"
    export EVAL_BATCH_SIZE="${EVAL_BATCH_SIZE:-8}"
    export MODALITY_CYCLE="${MODALITY_CYCLE:-text,image,image,image,speech,speech}"
    export CAPACITY_FACTOR="${CAPACITY_FACTOR:-8.0}"
    export AUX_COEF="${AUX_COEF:-0.01}"
    export TRAIN_LM_HEAD="${TRAIN_LM_HEAD:-1}"
    export LEARNING_RATE="${LEARNING_RATE:-0.0008}"
    export LM_HEAD_LEARNING_RATE="${LM_HEAD_LEARNING_RATE:-0.00001}"
    export IMAGE_PREFIX_TOKENS="${IMAGE_PREFIX_TOKENS:-75}"
    export AUDIO_PREFIX_TOKENS="${AUDIO_PREFIX_TOKENS:-64}"
    export ENCODER_FEATURE_TOKENS="${ENCODER_FEATURE_TOKENS:-100}"
    export CONDITIONAL_NEGATIVES="${CONDITIONAL_NEGATIVES:-9}"
    export CONDITIONAL_RANKING_NEGATIVES="${CONDITIONAL_RANKING_NEGATIVES:-9}"
    export CONDITIONAL_RANKING_TEMPERATURE="${CONDITIONAL_RANKING_TEMPERATURE:-0.7}"
    export IMAGE_CONDITIONAL_RANKING_COEF="${IMAGE_CONDITIONAL_RANKING_COEF:-3.0}"
    export SPEECH_CONDITIONAL_RANKING_COEF="${SPEECH_CONDITIONAL_RANKING_COEF:-1.5}"
    ;;
  contrastive-light-cap6)
    export JOB_NAME="${JOB_NAME:-sparse-moe-clean-e3-contrastive-light-cap6-${STAMP}}"
    export OUT="${OUT:-outputs/${JOB_NAME}}"
    export FINAL_STEPS="${FINAL_STEPS:-4500}"
    export TRAIN_BATCH_SIZE="${TRAIN_BATCH_SIZE:-4}"
    export EVAL_BATCH_SIZE="${EVAL_BATCH_SIZE:-8}"
    export MODALITY_CYCLE="${MODALITY_CYCLE:-text,image,speech,speech,speech}"
    export CAPACITY_FACTOR="${CAPACITY_FACTOR:-6.0}"
    export AUX_COEF="${AUX_COEF:-0.01}"
    export TRAIN_LM_HEAD="${TRAIN_LM_HEAD:-1}"
    export LEARNING_RATE="${LEARNING_RATE:-0.0006}"
    export LM_HEAD_LEARNING_RATE="${LM_HEAD_LEARNING_RATE:-0.00001}"
    export RETRIEVAL_HEAD_LEARNING_RATE="${RETRIEVAL_HEAD_LEARNING_RATE:-0.001}"
    export CONTRASTIVE_COEF="${CONTRASTIVE_COEF:-0.2}"
    export IMAGE_CONTRASTIVE_COEF="${IMAGE_CONTRASTIVE_COEF:-0.5}"
    export SPEECH_CONTRASTIVE_COEF="${SPEECH_CONTRASTIVE_COEF:-0.5}"
    export IMAGE_CONTRASTIVE_NEGATIVES="${IMAGE_CONTRASTIVE_NEGATIVES:--1}"
    export SPEECH_CONTRASTIVE_NEGATIVES="${SPEECH_CONTRASTIVE_NEGATIVES:--1}"
    export CONDITIONAL_NEGATIVES="${CONDITIONAL_NEGATIVES:-9}"
    export CONDITIONAL_RANKING_NEGATIVES="${CONDITIONAL_RANKING_NEGATIVES:-9}"
    export CONDITIONAL_RANKING_TEMPERATURE="${CONDITIONAL_RANKING_TEMPERATURE:-0.7}"
    export IMAGE_CONDITIONAL_RANKING_COEF="${IMAGE_CONDITIONAL_RANKING_COEF:-1.0}"
    export SPEECH_CONDITIONAL_RANKING_COEF="${SPEECH_CONDITIONAL_RANKING_COEF:-2.0}"
    ;;

  contrastive-imageboost-cap6)
    export JOB_NAME="${JOB_NAME:-sparse-moe-clean-e3-contrastive-imageboost-cap6-${STAMP}}"
    export OUT="${OUT:-outputs/${JOB_NAME}}"
    export FINAL_STEPS="${FINAL_STEPS:-4500}"
    export TRAIN_BATCH_SIZE="${TRAIN_BATCH_SIZE:-4}"
    export EVAL_BATCH_SIZE="${EVAL_BATCH_SIZE:-8}"
    export MODALITY_CYCLE="${MODALITY_CYCLE:-text,image,image,speech,speech,speech}"
    export CAPACITY_FACTOR="${CAPACITY_FACTOR:-6.0}"
    export AUX_COEF="${AUX_COEF:-0.01}"
    export TRAIN_LM_HEAD="${TRAIN_LM_HEAD:-1}"
    export LEARNING_RATE="${LEARNING_RATE:-0.0006}"
    export LM_HEAD_LEARNING_RATE="${LM_HEAD_LEARNING_RATE:-0.00001}"
    export RETRIEVAL_HEAD_LEARNING_RATE="${RETRIEVAL_HEAD_LEARNING_RATE:-0.001}"
    export CONTRASTIVE_COEF="${CONTRASTIVE_COEF:-0.2}"
    export IMAGE_CONTRASTIVE_COEF="${IMAGE_CONTRASTIVE_COEF:-0.5}"
    export SPEECH_CONTRASTIVE_COEF="${SPEECH_CONTRASTIVE_COEF:-0.5}"
    export IMAGE_CONTRASTIVE_NEGATIVES="${IMAGE_CONTRASTIVE_NEGATIVES:--1}"
    export SPEECH_CONTRASTIVE_NEGATIVES="${SPEECH_CONTRASTIVE_NEGATIVES:--1}"
    export CONDITIONAL_NEGATIVES="${CONDITIONAL_NEGATIVES:-4}"
    export CONDITIONAL_RANKING_NEGATIVES="${CONDITIONAL_RANKING_NEGATIVES:-9}"
    export CONDITIONAL_RANKING_TEMPERATURE="${CONDITIONAL_RANKING_TEMPERATURE:-0.7}"
    export IMAGE_CONDITIONAL_RANKING_COEF="${IMAGE_CONDITIONAL_RANKING_COEF:-2.0}"
    export SPEECH_CONDITIONAL_RANKING_COEF="${SPEECH_CONDITIONAL_RANKING_COEF:-2.0}"
    ;;

  contrastive-balanced-cap8)
    export JOB_NAME="${JOB_NAME:-sparse-moe-clean-e3-contrastive-balanced-cap8-${STAMP}}"
    export OUT="${OUT:-outputs/${JOB_NAME}}"
    export FINAL_STEPS="${FINAL_STEPS:-4500}"
    export TRAIN_BATCH_SIZE="${TRAIN_BATCH_SIZE:-4}"
    export EVAL_BATCH_SIZE="${EVAL_BATCH_SIZE:-8}"
    export MODALITY_CYCLE="${MODALITY_CYCLE:-text,image,image,speech,speech,speech}"
    export CAPACITY_FACTOR="${CAPACITY_FACTOR:-8.0}"
    export AUX_COEF="${AUX_COEF:-0.01}"
    export TRAIN_LM_HEAD="${TRAIN_LM_HEAD:-1}"
    export LEARNING_RATE="${LEARNING_RATE:-0.0006}"
    export LM_HEAD_LEARNING_RATE="${LM_HEAD_LEARNING_RATE:-0.00001}"
    export RETRIEVAL_HEAD_LEARNING_RATE="${RETRIEVAL_HEAD_LEARNING_RATE:-0.001}"
    export CONTRASTIVE_COEF="${CONTRASTIVE_COEF:-0.2}"
    export IMAGE_CONTRASTIVE_COEF="${IMAGE_CONTRASTIVE_COEF:-0.5}"
    export SPEECH_CONTRASTIVE_COEF="${SPEECH_CONTRASTIVE_COEF:-0.5}"
    export IMAGE_CONTRASTIVE_NEGATIVES="${IMAGE_CONTRASTIVE_NEGATIVES:--1}"
    export SPEECH_CONTRASTIVE_NEGATIVES="${SPEECH_CONTRASTIVE_NEGATIVES:--1}"
    export CONDITIONAL_NEGATIVES="${CONDITIONAL_NEGATIVES:-4}"
    export CONDITIONAL_RANKING_NEGATIVES="${CONDITIONAL_RANKING_NEGATIVES:-9}"
    export CONDITIONAL_RANKING_TEMPERATURE="${CONDITIONAL_RANKING_TEMPERATURE:-0.7}"
    export IMAGE_CONDITIONAL_RANKING_COEF="${IMAGE_CONDITIONAL_RANKING_COEF:-2.5}"
    export SPEECH_CONDITIONAL_RANKING_COEF="${SPEECH_CONDITIONAL_RANKING_COEF:-2.5}"
    ;;

  contrastive-img15-cap6)
    export JOB_NAME="${JOB_NAME:-sparse-moe-clean-e3-contrastive-img15-cap6-${STAMP}}"
    export OUT="${OUT:-outputs/${JOB_NAME}}"
    export FINAL_STEPS="${FINAL_STEPS:-4500}"
    export TRAIN_BATCH_SIZE="${TRAIN_BATCH_SIZE:-4}"
    export EVAL_BATCH_SIZE="${EVAL_BATCH_SIZE:-8}"
    export MODALITY_CYCLE="${MODALITY_CYCLE:-text,image,speech,speech,speech}"
    export CAPACITY_FACTOR="${CAPACITY_FACTOR:-6.0}"
    export AUX_COEF="${AUX_COEF:-0.01}"
    export TRAIN_LM_HEAD="${TRAIN_LM_HEAD:-1}"
    export LEARNING_RATE="${LEARNING_RATE:-0.0006}"
    export LM_HEAD_LEARNING_RATE="${LM_HEAD_LEARNING_RATE:-0.00001}"
    export RETRIEVAL_HEAD_LEARNING_RATE="${RETRIEVAL_HEAD_LEARNING_RATE:-0.001}"
    export CONTRASTIVE_COEF="${CONTRASTIVE_COEF:-0.2}"
    export IMAGE_CONTRASTIVE_COEF="${IMAGE_CONTRASTIVE_COEF:-0.5}"
    export SPEECH_CONTRASTIVE_COEF="${SPEECH_CONTRASTIVE_COEF:-0.5}"
    export IMAGE_CONTRASTIVE_NEGATIVES="${IMAGE_CONTRASTIVE_NEGATIVES:--1}"
    export SPEECH_CONTRASTIVE_NEGATIVES="${SPEECH_CONTRASTIVE_NEGATIVES:--1}"
    export CONDITIONAL_NEGATIVES="${CONDITIONAL_NEGATIVES:-4}"
    export CONDITIONAL_RANKING_NEGATIVES="${CONDITIONAL_RANKING_NEGATIVES:-9}"
    export CONDITIONAL_RANKING_TEMPERATURE="${CONDITIONAL_RANKING_TEMPERATURE:-0.7}"
    export IMAGE_CONDITIONAL_RANKING_COEF="${IMAGE_CONDITIONAL_RANKING_COEF:-1.5}"
    export SPEECH_CONDITIONAL_RANKING_COEF="${SPEECH_CONDITIONAL_RANKING_COEF:-2.0}"
    ;;

  contrastive-prefix75-cap8)
    export JOB_NAME="${JOB_NAME:-sparse-moe-clean-e3-contrastive-prefix75-cap8-${STAMP}}"
    export OUT="${OUT:-outputs/${JOB_NAME}}"
    export FINAL_STEPS="${FINAL_STEPS:-4500}"
    export TRAIN_BATCH_SIZE="${TRAIN_BATCH_SIZE:-4}"
    export EVAL_BATCH_SIZE="${EVAL_BATCH_SIZE:-8}"
    export MODALITY_CYCLE="${MODALITY_CYCLE:-text,image,image,speech,speech,speech}"
    export CAPACITY_FACTOR="${CAPACITY_FACTOR:-8.0}"
    export AUX_COEF="${AUX_COEF:-0.01}"
    export TRAIN_LM_HEAD="${TRAIN_LM_HEAD:-1}"
    export LEARNING_RATE="${LEARNING_RATE:-0.0006}"
    export LM_HEAD_LEARNING_RATE="${LM_HEAD_LEARNING_RATE:-0.00001}"
    export RETRIEVAL_HEAD_LEARNING_RATE="${RETRIEVAL_HEAD_LEARNING_RATE:-0.001}"
    export IMAGE_PREFIX_TOKENS="${IMAGE_PREFIX_TOKENS:-75}"
    export AUDIO_PREFIX_TOKENS="${AUDIO_PREFIX_TOKENS:-64}"
    export ENCODER_FEATURE_TOKENS="${ENCODER_FEATURE_TOKENS:-100}"
    export CONTRASTIVE_COEF="${CONTRASTIVE_COEF:-0.2}"
    export IMAGE_CONTRASTIVE_COEF="${IMAGE_CONTRASTIVE_COEF:-0.5}"
    export SPEECH_CONTRASTIVE_COEF="${SPEECH_CONTRASTIVE_COEF:-0.5}"
    export IMAGE_CONTRASTIVE_NEGATIVES="${IMAGE_CONTRASTIVE_NEGATIVES:--1}"
    export SPEECH_CONTRASTIVE_NEGATIVES="${SPEECH_CONTRASTIVE_NEGATIVES:--1}"
    export CONDITIONAL_NEGATIVES="${CONDITIONAL_NEGATIVES:-4}"
    export CONDITIONAL_RANKING_NEGATIVES="${CONDITIONAL_RANKING_NEGATIVES:-9}"
    export CONDITIONAL_RANKING_TEMPERATURE="${CONDITIONAL_RANKING_TEMPERATURE:-0.7}"
    export IMAGE_CONDITIONAL_RANKING_COEF="${IMAGE_CONDITIONAL_RANKING_COEF:-1.5}"
    export SPEECH_CONDITIONAL_RANKING_COEF="${SPEECH_CONDITIONAL_RANKING_COEF:-2.5}"
    ;;

  contrastive-router-cap8)
    export JOB_NAME="${JOB_NAME:-sparse-moe-clean-e3-contrastive-router-cap8-${STAMP}}"
    export OUT="${OUT:-outputs/${JOB_NAME}}"
    export FINAL_STEPS="${FINAL_STEPS:-4500}"
    export TRAIN_BATCH_SIZE="${TRAIN_BATCH_SIZE:-4}"
    export EVAL_BATCH_SIZE="${EVAL_BATCH_SIZE:-8}"
    export MODALITY_CYCLE="${MODALITY_CYCLE:-text,image,speech,speech,speech}"
    export CAPACITY_FACTOR="${CAPACITY_FACTOR:-8.0}"
    export AUX_COEF="${AUX_COEF:-0.01}"
    export TRAIN_ROUTER_GATES=1
    export ROUTER_LEARNING_RATE="${ROUTER_LEARNING_RATE:-0.00001}"
    export TRAIN_LM_HEAD="${TRAIN_LM_HEAD:-1}"
    export LEARNING_RATE="${LEARNING_RATE:-0.0006}"
    export LM_HEAD_LEARNING_RATE="${LM_HEAD_LEARNING_RATE:-0.00001}"
    export RETRIEVAL_HEAD_LEARNING_RATE="${RETRIEVAL_HEAD_LEARNING_RATE:-0.001}"
    export CONTRASTIVE_COEF="${CONTRASTIVE_COEF:-0.2}"
    export IMAGE_CONTRASTIVE_COEF="${IMAGE_CONTRASTIVE_COEF:-0.5}"
    export SPEECH_CONTRASTIVE_COEF="${SPEECH_CONTRASTIVE_COEF:-0.5}"
    export IMAGE_CONTRASTIVE_NEGATIVES="${IMAGE_CONTRASTIVE_NEGATIVES:--1}"
    export SPEECH_CONTRASTIVE_NEGATIVES="${SPEECH_CONTRASTIVE_NEGATIVES:--1}"
    export CONDITIONAL_NEGATIVES="${CONDITIONAL_NEGATIVES:-4}"
    export CONDITIONAL_RANKING_NEGATIVES="${CONDITIONAL_RANKING_NEGATIVES:-9}"
    export CONDITIONAL_RANKING_TEMPERATURE="${CONDITIONAL_RANKING_TEMPERATURE:-0.7}"
    export IMAGE_CONDITIONAL_RANKING_COEF="${IMAGE_CONDITIONAL_RANKING_COEF:-1.25}"
    export SPEECH_CONDITIONAL_RANKING_COEF="${SPEECH_CONDITIONAL_RANKING_COEF:-2.0}"
    ;;
  *)
    echo "unknown candidate: $CANDIDATE" >&2
    exit 2
    ;;
esac

export FEATURE_CACHE_DIR="${FEATURE_CACHE_DIR:-${OUT}/feature_cache}"

echo "Submitting clean E3 candidate $CANDIDATE as $JOB_NAME -> $OUT"
bash scripts/submit_e3_candidate_runai.sh
